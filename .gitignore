node_modules/
.next/
out/
build/
dist/

# Ignore every env variant (incl. .env.production / .env.development); keep the
# committed template so no real secret file can be added by accident.
.env
.env.*
!.env.example

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

.vercel
.turbo
.cache

.DS_Store
Thumbs.db
__pycache__/
*.pyc
*.tsbuildinfo
next-env.d.ts.bak

coverage/
test-results/
playwright-report/
# Separate repo; do not nest as submodule unless intentional
AERIS CHAT/06 AERIS CHAT/

# Agent scratch: .claude/worktrees holds full checkouts of this repo, which
# both bloat the tree and get picked up by jest/tsc if ever committed.
.claude/
.cursor/
supabase/.temp/

# Raw GIS source data (~570 MB of .shp/.zip). These are one-time build inputs
# for scripts/convert_flood_shapefile.py; the runtime artifacts it produces
# live in public/flood-hazard/. Keep the originals in object storage, not git.
mapfiles/

# Local scratch that has accumulated in the repo root.
*.crdownload
body.txt
overpass_test.json
